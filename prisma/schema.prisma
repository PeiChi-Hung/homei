generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model sample_address {
  id              Int       @id @default(autoincrement())
  sold_price      Int?     
  address         String  
  suburb          String  
  state           String
  postcode        Int
  full_address    String
}

